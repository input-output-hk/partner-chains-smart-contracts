#
# A Makefile to wrap `./mdcbo.pl`.
#
# Executing `make` will take `Tutorial.template.md` and generate
# `Tutorial.md` which is identical to `Tutorial.template.md` but
# includes some sample outputs from the example shell commands. For details,
# please see `./mdcbo.pl`.
#
# So, all edits to `Tutorial.md` should instead happen on
# `Tutorial.template.md`, and to update `Tutorial.md`, type:
# ```
# make
# ```
#

.PHONY: clean

Tutorial.md: Tutorial.template.md mdcbo.pl
	perl mdcbo.pl -o $@ $<
	sed -i -e '1s#^#<!-- WARNING: do NOT modify this autogenerated file. See `./Makefile` for details. -->\n#' $@

clean:
	-rm *.json
	-rm *.mdcbo.sh
	-rm *.log
	-rm Tutorial.md
